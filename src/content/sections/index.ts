// Section registry — append to this array to add a new section to the live site.
import type { SectionContent } from "./types";
import { mlEngineering } from "./ml-engineering";

export const sections: SectionContent[] = [
  mlEngineering,
];
